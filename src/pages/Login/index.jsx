import styled from "styled-components";

const StyledDefaultText = styled.div`
    padding: 100px;
`

function Login(){
    return(
        <StyledDefaultText>
            Login
        </StyledDefaultText>
    )
}

export default Login;